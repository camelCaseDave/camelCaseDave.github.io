export default class ExperienceCard {
    constructor(company, duration, tasks, footer) {
      this.Company = company;
      this.Duration = duration;
      this.Tasks = tasks;
      this.Footer = footer;
    }
  }