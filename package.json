{
  "name": "gh-pages",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://camelcasedave.github.io/",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-flip-move": "^3.0.1",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git push origin `git subtree split --prefix build develop`:master --force"
  }
}
